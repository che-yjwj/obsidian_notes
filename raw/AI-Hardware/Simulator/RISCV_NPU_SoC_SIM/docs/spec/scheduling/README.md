# Scheduling Spec Index

이 디렉터리는 Offline Compiler의 스케줄링 단계가 생성해야 하는 **규범(semantics)** 을 정의한다.

## 문서 목록

- `static_scheduler_semantics_spec.md` — TileGraph/TDG 기반 정적 스케줄의 최소 의미론(결정론/ID/의존성 규칙)
- `prefill_decode_workload_semantics_spec.md` — LLM Prefill/Decode 워크로드 매핑의 최소 의미론(금지 패턴/관측 조건)
